# Build script for creating iOS applications

That is simple alternative of [Fastlane](https://github.com/fastlane/fastlane)

# Usage

## Help

```
sh build.sh --help
```

## Example

```
sh build.sh -p ProjectName -ip -t --version auto
```
